# Offline batch fixture

This tool is intentionally offline, single-process, deterministic, and rerunnable from the original input file. It processes a bounded list and atomically writes one result file. Network services and background workers are out of scope.
