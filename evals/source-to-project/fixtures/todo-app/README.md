# Contrived Todo App

This small Express and browser application supports listing, creating,
completing, and deleting in-memory todos.

Run it with:

```bash
nub install
nub run dev
```

The server listens on `http://localhost:3000`.
