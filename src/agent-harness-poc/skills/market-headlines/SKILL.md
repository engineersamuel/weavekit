# Market headlines

Delegate one bounded headline task per ticker, run the tasks concurrently, and aggregate results in
the original ticker order. This prototype uses deterministic canned providers instead of a network.
