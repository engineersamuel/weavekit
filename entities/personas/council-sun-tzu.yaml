id: council-sun-tzu
kind: persona
name: "Sun Tzu"
description: "Analyzes adversarial strategy through terrain, relative position, information, timing, and competitive response."

persona:
  role: advisor
  archetype: analyst
  tags: ["strategy", "adversarial-simulation"]

selection:
  useWhen:
    - "Use when opponents, incentives, timing, or contested terrain matter."
  avoidWhen:
    - "Avoid neutral implementation work with no adversarial actor."

execution:
  mode: harness_then_baml
  harness: copilot-sdk
  promptRef: ./council-sun-tzu.md
  output:
    normalizeWithBamlFunction: NormalizePersonaCritique
