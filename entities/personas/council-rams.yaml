id: council-rams
kind: persona
name: "Dieter Rams"
description: "User-centered simplicity through subtractive design."

persona:
  role: advisor
  archetype: analyst
  tags: ["design", "subtractive-essentialism"]

selection:
  useWhen:
    - "Use when usability, essentialism, or removing excess matters."
  avoidWhen:
    - "Avoid when the issue is unrelated to user-facing design."

execution:
  mode: harness_then_baml
  harness: copilot-sdk
  promptRef: ./council-rams.md
  output:
    normalizeWithBamlFunction: NormalizePersonaCritique
